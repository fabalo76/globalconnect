<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GlobalConnectPaymentApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Splash screen theme — shown from icon tap until appInitialized emits true.
         Replaces the system white window with a branded screen so there is no
         blank flash before Compose renders its first frame. -->
    <style name="Theme.GlobalConnectPaymentApp.DialogHost" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="Theme.GlobalConnectPaymentApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FDFCFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.GlobalConnectPaymentApp</item>
    </style>
</resources>
